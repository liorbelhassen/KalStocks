node_modules
dist
.env
.env.local
.env.*.local
.firebase
*.log
.DS_Store

# Firebase service-account keys — NEVER commit
serviceAccount*.json
*service-account*.json
*-firebase-adminsdk-*.json

# Server-only API keys for local testing (Gemini/Telegram/Resend) — NEVER commit
.secrets.env
